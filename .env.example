# MongoDB connection string used by server.js
MONGO_URI=mongodb://127.0.0.1:27017/url-shortener

# Public base URL of the deployed backend (Render). If unset, the backend
# derives the short-link host from each request. Set this to a stable domain.
# Example: https://your-app.onrender.com
SHORT_URL_BASE=
