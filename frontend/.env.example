# Base URL of the deployed backend (Render). Used in production so the frontend
# calls the API cross-origin instead of relying on a dev proxy.
# Example: https://your-app.onrender.com
VITE_API_BASE_URL=
